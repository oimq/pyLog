from setuptools import setup, find_packages

setup(name="pyLog",
      version=1.0,
      url='https://github.com/oimq/pyLog',
      author="oimq",
      author_email='taep0q@gmail.com',
      description='Logging in Python',
      packages=find_packages(),
      install_requires=[],
      zip_safe=False
)